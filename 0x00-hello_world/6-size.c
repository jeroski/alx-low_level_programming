#include <stdio.h>
/**
 * main - main block
 * Return: o
 */
int main(void)
{
	printf("size of a char is=%i bity,\n"sizeof (char);
	 printf("size of an int is=%i bitys,\n"sizeof (int);
	  printf("size of a long int is=%i bitys,\n"sizeof (long int);
	   printf("size of a long long int is=%i bitys,\n"sizeof (long long int);
	    printf("size of a float is=%i bitys,\n"sizeof (float);
	return (0);
}
